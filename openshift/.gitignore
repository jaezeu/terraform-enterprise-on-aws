# Machine-local artifacts — regenerated by scripts/bootstrap.sh.
# install-dir/ is radioactive: ignition embeds the pull secret and every
# cluster cert/key; auth/ holds the admin kubeconfig. NEVER COMMIT.
.bin/
install-dir/
ocp-ssh-key*
